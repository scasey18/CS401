(if (equal? (first exp) `lambda)
      (if (equal? (second exp)
                  (if (list? (third exp))
                      (third exp)
                      (list (third exp))
                      )
                  )
          #f
          (third exp)
          )
      (if (reduce-lambda (first exp))
          (reduce-lambda (first exp))
          (second exp)
          )
      )